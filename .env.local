# URL of your local API server exposed via Cloudflare Tunnel
# Replace with your actual tunnel URL after setup
# For local development: http://localhost:3001
NEXT_PUBLIC_API_URL=http://localhost:3001

# URL of your local HLS server exposed via Cloudflare Tunnel
# Replace with your actual tunnel URL after setup
# For local development: http://localhost:3002
NEXT_PUBLIC_HLS_URL=http://localhost:3002
